<Page
    x:Class="LumiaImagingSDKPlayground.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LumiaImagingSDKPlayground"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <RelativePanel>
            <Button Width="60" Height="45" Name="hamburgerbtn" Click="hamburgerbtn_Click"  FontFamily="Segoe MDL2 Assets" Content="" Margin="0,30,0,0"/>
        </RelativePanel>
        <SplitView Name="splitPanel" Grid.Row="1" Grid.Column="0" CompactPaneLength="50" OpenPaneLength="300" IsPaneOpen="True">
            <SplitView.Content>
                <SwapChainPanel Name="mainDrawingArea" Width="800" Height="800" />
            </SplitView.Content>
            <SplitView.Pane>
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left">
                    <Button Content="OpenFile" Name="btnSelectFile" Tapped="btnSelectFile_Tapped"/>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>
    </Grid>
</Page>
 